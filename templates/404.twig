{% extends '_layouts/base.twig' %}

{% block bodyTag %}
  {{ parent()|attr({ class: 'bg-gray-200 h-screen' }) }}
{% endblock %}

{% block base %}
  <div class="flex h-full w-full items-center justify-center text-center">
    <div>
      <h1 class="font-mono text-2xl">404</h1>
      <div class="mt-4 font-mono">
        Page not found | <a class="text-blue-500" href="{{ siteUrl }}">home</a>
      </div>
    </div>
  </div>
{% endblock %}
