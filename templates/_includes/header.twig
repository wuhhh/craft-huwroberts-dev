<header>

  <div class="fixed top-0 inset-x-0 z-20 pointer-events-none pt-8 md:pt-[52px] 2xl:pt-[60px]">
    <div class="container">
      <a
        href="{{ siteUrl }}"
        aria-label="{{ siteUrl }} {{ "home"|t }}"
        class="size-[44px] border-2 border-black flex items-center justify-center md:size-14 md:border-[3px] 2xl:border-4 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-1 focus-visible:outline-blue-700"
      >
        {{ svg('@webroot/dist/images/h--logomark.svg', true, true)|attr({ class: 'w-[14px] md:w-[18px]' }) }}
      </a>
    </div>
  </div>

  {% include "_includes/headerNav" only %}

</header>
