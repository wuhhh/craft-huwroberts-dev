{% extends "_layouts/base.twig" %}

{% block bodyTag %}
  {{ parent()|attr({
    class: 'bg-gray-200 h-screen'
  }) }}
{% endblock %}

{% block base %}
  <div class="w-full h-full flex items-center justify-center text-center">
		<div>
			<h1 class="font-mono text-2xl">503</h1>
			<div class="font-mono mt-4">Temporarily offline</div>
		</div>
	</div>
{% endblock %}
